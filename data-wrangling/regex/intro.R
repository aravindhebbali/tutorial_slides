library(stringr)

# lesson 1
# \d can be used in place of any digit from 0 to 9
# \. can match any single character (wildcard)
# match specific characters by defining them inside []
# exclude specific characters using hat [^]
# a{3} will match the a character exactly 3 times
# a{1,3} will match the a character no more than 3 times, but no less than once

